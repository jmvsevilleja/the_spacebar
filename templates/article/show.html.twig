{% extends 'base.html.twig' %}

{# Title Block #}
{% block title %}
{{ title }}
{% endblock %}

{# Body Block #}
{% block body %}

{{dump()}}
<h1>{{ title }}</h1>
<h2>Comments: {{ comments|length }}</h2>
<ul>
	{% for comment in comments %}
	<li>{{ comment }}</li>
	{%endfor%}
</ul>
{% endblock %}
